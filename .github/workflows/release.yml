# vX.Y.Z タグのpushで macOS / Windows のインストーラーをビルドし、
# GitHub Release と自動アップデート用の latest.json を公開する
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: --target aarch64-apple-darwin
          - platform: macos-latest
            args: --target x86_64-apple-darwin
          - platform: windows-latest
            args: ""
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: dtolnay/rust-toolchain@stable
        with:
          # macOSはApple Silicon / Intel両方のバイナリを作る
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      # フロントエンドは静的ファイル (ui/) のみでnpmビルド不要のため、CLIだけ入れる
      - name: Install Tauri CLI
        run: npm install -g @tauri-apps/cli@^2

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # アップデーター署名鍵 (リポジトリのSecretsに登録しておく)
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # macOSコード署名 (Developer ID Application証明書の.p12をbase64化したもの)
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          # macOS公証 (notarization)。Apple ID + App用パスワードで行う
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          tauriScript: tauri
          tagName: ${{ github.ref_name }}
          releaseName: "Instagram CRM ${{ github.ref_name }}"
          releaseBody: "詳細は下記のAssetsからOSに合ったファイルをダウンロードしてください。macOS: .dmg / Windows: .msi"
          # 「-test」を含むタグ (例: v0.0.1-test) は下書きRelease (公開されず、自動アップデートにも配信されない)
          releaseDraft: ${{ contains(github.ref_name, '-test') }}
          args: ${{ matrix.args }}
